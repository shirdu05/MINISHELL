# MINISHELL
A lightweight Unix-like shell built in C that supports command execution, piping, and I/O redirection. Implements core Linux system calls like fork(), execvp(), and wait() to manage processes and mimic basic terminal functionality.
